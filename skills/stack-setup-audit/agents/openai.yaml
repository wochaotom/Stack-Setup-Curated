interface:
  display_name: "Agent Setup Audit"
  short_description: "Audit repo agent setup"
  default_prompt: "Use $stack-setup-audit to recommend the best cross-agent setup for this repository without editing files."

policy:
  allow_implicit_invocation: true
